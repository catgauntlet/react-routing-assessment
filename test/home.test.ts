describe("Home screen - Happy flows", () => {
    test('When I load the homepage component when I am not logged in, I expect to see the welcome text', () => {

    });
    
    test('When I load the homepage component when I am logged in, I expect to see the welcome text', () => {
    
    });

    test('When I load the homepage component when I am logged in, I expect the dashboard menu link to be clickable and to redirect me', () => {
    
    });

    test('When I load the homepage when I am logged in, I expect the logout button to be available and take me to the logged out state of the home page, I expect the token to be gone', () => {
    
    });
});