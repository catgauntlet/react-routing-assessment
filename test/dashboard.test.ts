
describe("Dashboard screen - Happy flows", () => {
    test('When I load the dashboard component when I am not logged in, I expect to be redirected to the home screen', () => {

    });
    
    test('When I load the dashboard component when I am logged in, based on the availability of the token, I expect the dashboard to be shown', () => {
    
    });

    test('When I load the dashboard when I am logged in, I expect the logout button to be available and take me to the logged out state of the home page, I expect the token to be gone', () => {
    
    });
});